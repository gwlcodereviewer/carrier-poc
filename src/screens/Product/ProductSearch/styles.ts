import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
	mainContainer: {
		flex: 1,
		paddingTop: 10,
	},
	headerContainer: {
		marginHorizontal: 24,
	},
});

export default styles;
